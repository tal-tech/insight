import React, { FC } from 'react';

import { Typography } from 'antd';

const DashBoardPage: FC = () => {
  return (
    <div>
      <Typography className="innerText">全埋点可视化 首页介绍</Typography>
    </div>
  );
};

export default DashBoardPage;
